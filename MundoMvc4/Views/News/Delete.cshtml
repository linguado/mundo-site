@using MundoMvc4.Globalization
@model NewsBlockLocal

@{
    ViewBag.Title = GDelete.Title;
}

<h2>@GDelete.Head</h2>

<h3>@String.Format(GDelete.Message,Model.Name)</h3>

@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="@GDelete.Submit" /> 
    </p>
}
