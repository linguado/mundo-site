@using MundoMvc4.Common
@using MundoMvc4.Globalization
@model GroupInfo

@{
    ViewBag.Title = Model.Name;
}

<div class="edit-menu-box"> [
    @Html.ActionLink(GGroups.Gyms, "Index", "Gyms", new {argument = Model.SysName}, null) |
    @if (User.IsInRole(MundoRoles.COACH))
    {
        <text>
            @Html.ActionLink(GActions.Edit, "Edit", "Groups", new {argument = Model.SysName}, null) |
            @Html.ActionLink(GActions.Delete, "Delete", "Groups", new {argument = Model.SysName}, null) [
            @Html.ActionLink("ru-Ru", "EditLocal", new {argument = Model.SysName, lng = "ru-Ru"})
            @Html.ActionLink("en-US", "EditLocal", new {argument = Model.SysName, lng = "en-US"})
            @Html.ActionLink("pt-Br", "EditLocal", new {argument = Model.SysName, lng = "pt-Br"}) ]
        </text>
    }
    @Html.ActionLink(GGyms.Schedule, "ForGroup", "Schedule", new {argument = Model.SysName}, null) 
</div>
    
<h2>@Model.Name</h2>
@Html.Raw(Model.Body)